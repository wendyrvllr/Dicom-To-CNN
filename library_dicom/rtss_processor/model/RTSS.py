from library_dicom.ROI_RTSS import ROI_RTSS

class RTSS(ROI_RTSS):
    """class to build RTSTRUCT 

    Arguments:
        ROI_RTSS {[class]} -- [informations about ROI RTSS]
    """

#Utiliser object Serie pour récupérer Patient Name, ID, Sex, etc etc + utiliser ROI_RTSS 